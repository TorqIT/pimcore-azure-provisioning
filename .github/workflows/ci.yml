name: Deploy and test changes in an Azure environment

on:
  pull_request:

concurrency: 
  group: ${{ github.repository }}__${{ github.ref }}-ci
  cancel-in-progress: true

jobs:
  build-and-push-image:
    uses: ./.github/workflows/cd.yml

  deploy-and-test-in-azure: 
    runs-on: ubuntu-latest
    needs: [build-and-push-image]
    container:
      image: ghcr.io/torqit/pimcore-azure-provisioning:${{ github.head_ref || github.ref_name }}
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.github_token }}
      volumes:
        - /var/run/docker.sock:/var/run/docker.sock
      options: --user root
      
    steps:
      - uses: actions/checkout@v4

      - run: cd /azure

      - name: Log in to Azure using Service Principal
        shell: bash
        run: |
          cat ${{ github.workspace }}/.github/parameters.json
          tenant=$(jq -r '.parameters.tenantId.value' ${{ github.workspace }}/.github/parameters.json)
          echo $tenant
          az login \
            --tenant $tenant \
            --service-principal \
              --username ${{ vars.SERVICE_PRINCIPAL_ID }} \
              --password ${{ secrets.SERVICE_PRINCIPAL_PASSWORD }}

      # - name: On opening of the PR, create the Resource Group
      #   if: github.event.action == 'opened'
      #   run: |
          
        
      # TODO if PR just created, create Resource Group
      # TODO deploy changes to resource group
      # TODO when PR is closed, delete Resource Group